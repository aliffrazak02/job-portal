{
  "name": "job-portal-e2e",
  "version": "1.0.0",
  "description": "Job Portal E2E tests (Playwright)",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
